name: client
version: 1.0.0+1
publish_to: none
description: A new Flutter project.
environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.2

  #State Managemet, Dependency injection and routing:
  get: 4.3.8

  #Firebase:
  firebase_core: ^1.10.0
  firebase_auth: ^3.2.0
  firebase_storage: ^10.2.10
  cloud_firestore: ^3.1.11

  #Utility
  logger: ^1.1.0
  cached_network_image: ^3.2.0
  image_picker: ^0.8.5

  #UI
  animations: ^2.0.2
  salomon_bottom_bar: ^3.1.0

  flutter:
    sdk: flutter

dependency_overrides:

dev_dependencies:
  flutter_lints: ^1.0.4

  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    # - assets/images/
    - assets/icons/
    # - assets/json/

get_cli:
  sub_folder: false
